[package]
name = "bevy-physics-weekend"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18", default-features = false, features = ["3d"] }
bevy_flycam = "0.18.0"
bevy_mesh = "0.18"
physics = { path = "physics" }
